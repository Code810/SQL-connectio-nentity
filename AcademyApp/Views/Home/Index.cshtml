@using AcademyApp.Models;
@model List<Group>

<h1>Home index page</h1>

<ul>
    @foreach(var group in Model)
    {
        <li> 
            <a href="/student/index?groupId=@group.Id">@group.Name</a>
        </li>
    }
</ul>